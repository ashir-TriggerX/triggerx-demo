{
  "name": "triggerx-demo",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "description": "TriggerX — interactive outbound intelligence demo (frontend only).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.2"
  }
}
